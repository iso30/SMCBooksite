<p><%= flash[:danger]%></p>
<%= form_for :session, url: login_path do |f| %>
	<%= f.label "Username:" %>
	<%= f.text_field :username %>
	<%= f.label "Password:" %>
	<%= f.password_field :password %>
	<%= f.submit "Login" %>
<% end %>